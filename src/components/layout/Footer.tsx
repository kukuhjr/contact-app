import { css } from '@emotion/css'

const Footer = () => {
    return (
        <div
            className={css`
                padding: 1.5rem 0;
                display: flex;
                justify-content: center;
                align-items: center;
                background: pink;

            `}
        >
            Footer
        </div>
    )
}

export default Footer