export interface CharacterProps {
    gender: string;
    id: string;
    image: string;
    name: string;
    __typename: string;
}

export interface EpisodeProps {
    name: string;
    episode: string;
}